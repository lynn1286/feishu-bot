# 背景

文件名：2026-01-16_2
创建于：2026-01-16
创建者：Lynn
主分支：main
任务分支：task/fix-timezone_2026-01-16_2
Yolo 模式：Off

# 任务描述

检查下原始 payload 里面有没有关于地区的说明，当前时间好像跟本地相差 8 个小时了。

# 分析

payload 中包含 UTC 时间戳。后端数据库存储的时间可能缺少时区信息，导致前端按本地时间解析，产生了时差。

# 提议的解决方案

前端 `history.tsx` 中增加 `parseDate` 函数，强制将无时区后缀的时间字符串视为 UTC 时间解析。

# 任务进度

2026-01-16 - 任务初始化，开始调查。
2026-01-16 - [EXECUTE] 修改 `web/src/pages/history.tsx`，修复时区解析问题。

# 最终审查

实施与计划完全匹配。增加了 `parseDate` 函数，正确处理了后端返回的 UTC 时间。
